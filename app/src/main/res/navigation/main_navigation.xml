<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/main_navigation"
            app:startDestination="@+id/splash">

    <fragment android:id="@+id/splash"
              android:name="com.saivo.recommendo.ui.SplashFragment"
              android:label="fragment_splash"
              tools:layout="@layout/fragment_splash">
        <action app:destination="@id/login" android:id="@+id/to_login_action"/>
        <action android:id="@+id/splash_to_main_ui_action" app:destination="@id/main_ui"/>
    </fragment>
    <fragment android:id="@+id/login"
              android:name="com.saivo.recommendo.ui.auth.LoginFragment"
              android:label="fragment_login"
              tools:layout="@layout/fragment_login">
        <action android:id="@+id/login_to_register_action" app:destination="@id/register"/>
        <action android:id="@+id/login_to_main_ui_action" app:destination="@id/main_ui"/>
        <action android:id="@+id/login_to_reset_password_action" app:destination="@id/reset_password"/>
    </fragment>
    <fragment android:id="@+id/register"
              android:name="com.saivo.recommendo.ui.auth.RegisterFragment"
              android:label="fragment_register"
              tools:layout="@layout/fragment_register">
        <action app:destination="@id/main_ui" android:id="@+id/register_to_main_ui_action"/>
    </fragment>
    <fragment android:id="@+id/main_ui"
              android:name="com.saivo.recommendo.ui.MainFragment"
              android:label="fragment_main"
              tools:layout="@layout/fragment_main"/>
    <fragment android:id="@+id/reset_password" android:name="com.saivo.recommendo.ui.auth.ResetPasswordFragment"
              android:label="fragment_reset_password" tools:layout="@layout/fragment_reset_password">
        <action android:id="@+id/reset_password_to_login_action" app:destination="@id/login" app:popUpTo="@id/login"/>
    </fragment>
</navigation>